#pragma once

// OSafeThread — the in-game settings page, built on the SKSE Menu Framework (v3, Mod Control Panel).
// Register() is a no-op when the framework isn't installed, so it's always safe to call.

namespace OSafeThread::UI
{
    void Register();
}
